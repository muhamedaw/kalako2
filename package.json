{
  "name": "kalako",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Starting server and client...' && cmd /c \"cd server && npx tsx server.mts\" & cmd /c \"cd kalako-client && npx vite\"",
    "dev:server": "cd server && npx tsx server.mts",
    "dev:client": "cd kalako-client && npx vite",
    "build": "cd kalako-client && npx vite build",
    "deploy": "cd kalako-client && npx vercel deploy -y --no-wait"
  }
}
